const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on('console', msg => console.log('PAGE LOG:', msg.text())); await page.goto('http://localhost:3000/submit-inspection', {waitUntil: 'networkidle0'}); await page.click('button:has-text(\Next\)'); await page.waitForSelector('input[name=\insuranceCompany\]'); await page.click('input[name=\insuranceCompany\]'); await new Promise(r => setTimeout(r, 3000)); await browser.close(); })();
