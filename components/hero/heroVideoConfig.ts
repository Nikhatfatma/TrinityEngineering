/** Shared hero background video — used on Home, Claims, SWI, Fortified */

/** Compressed H.264 MP4 (~23 MB, faststart) — preferred for web */
export const HERO_VIDEO_MP4 = "/hero-video.mp4";

/** Original MOV fallback if MP4 missing */
export const HERO_VIDEO_MOV = "/Trinity%20Engineering%20Hero%20Video%20VER2.mov";

/** Static fallback when prefers-reduced-motion */
export const HERO_VIDEO_FALLBACK_IMAGE = "/hero-background.png";
